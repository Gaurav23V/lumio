{
  "name": "@lumio/infra-supabase",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "eslint tests --ext .ts",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run --project integration",
    "build": "echo \"No build artifacts for infra\""
  }
}
