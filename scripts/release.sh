#!/usr/bin/env bash
# Lumio release workflow (placeholder)
# Builds desktop artifacts for Linux and Windows, prepares for distribution.
set -e
echo "Lumio release script (placeholder)"
echo "TODO: Build desktop app, sign artifacts, upload to CDN/install endpoints"
echo "Local build: pnpm --filter @lumio/desktop tauri:build"
exit 1
